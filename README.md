# resume-radar

Resume reader is an AI powered platform that allows an user to upload a resume and a job description and find out the scoring, gaps and suggestions

The client folder contains the front end and uses React, TypeScript and TailwindCSS

The front end can be started by cd client && npm run dev